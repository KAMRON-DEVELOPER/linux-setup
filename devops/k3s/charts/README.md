
# Chart pull commands

> kube-prometheus-stack

```bash
helm pull prometheus-community/kube-prometheus-stack --untar --untardir k3s/charts/prometheus-community-manifests
```

> opentelemetry-collector

```bash
helm pull open-telemetry/opentelemetry-collector --untar --untardir k3s/charts/open-telemetry-manifests
```

> loki-stack

```bash
helm pull grafana/loki-stack --untar --untardir k3s/charts/grafana-manifests
```

```bash
helm pull open-telemetry/opentelemetry-collector --untar --untardir k3s/charts/open-telemetry-manifests
```
