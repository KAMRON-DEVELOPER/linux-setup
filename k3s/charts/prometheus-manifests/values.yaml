grafana:
  adminPassword: admin
prometheus:
  prometheusSpec:
    storageSpec:
      volumeClaimTemplate:
        spec:
          resources:
            requests:
              storage: 10Gi

kube-state-metrics:
  metricLabelsAllowlist:
    - pods=[managed-by,deployment-id]
    - deployments=[managed-by,deployment-id]
